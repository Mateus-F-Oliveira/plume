import 'package:flutter/material.dart';
import 'dart:core';

class NavigationButtonWidget extends StatelessWidget
{
  const NavigationButtonWidget({super.key});
  @override
  Widget build(BuildContext context) => Container();
}